"use stricts"; //treat all JS code as newer version

//alert( 3 + 3) // we are using NodeJS not browsers

console.log(3 + 3) // code readability should be high

console.log("Anmol")

let name = "Anmol"
let age = 18
let isLoggedIn = false
let state;
// number => 2 to power 53 (range of numbers in memory Address)
// bigint (use for Big Value of intigers for increase number range in Memory Address)
// string => "" (use can be single or double but preferable is Doublese "")
// boolean => true/false 
// null => StandAlone Value
// undefined =>(when value is not no define )
// symbols => unique


// object

console.log(typeof null);
console.log(typeof undefined); 